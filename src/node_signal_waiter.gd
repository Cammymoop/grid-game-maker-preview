extends Object

#signal done

# Might make this later, the idea is to give it a node and a signal to wait for
# if the signal is called it will pass the args through (this is the impossible part with no way to take variadic arguements)
# if the node is removed from the tree, then don is emitted with no value
#
#func wait_on(node, signal_name) -> void:
#
#
#func callback(args)
