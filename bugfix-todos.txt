Missing 'Duplicate Step' button

some tooltips (like slot buttons in conditional editor) have dark text on dark background

After saving level, still complains about needing to save changes when exiting
Save confirm when leaving a level while testing saves current state of level not edited state

First new level in empty game not assigned to 'Levels' list by default

Add level list button disabled when no lists? (test on sandy or broken abacus)

line/solid scroll and warp scroll angles in style editor are huge???

paste sprite context menu option missing

selecting sae to list in save level dialog has no effect


background falling sprites not notified properly when deleting entity

fix is_entity_at command

Options menu doesn't grab focus for controller compatibility


missing commands:
  is tile named (tile with name exists in positions)
  select first tile from a set of multiple (or nearest?)
  create entity using spawn effect/play spawn effect on already created entity
  if entity with true property at




Stop repeat after bonk prevents further movement at all in discrete mode


(low priority)
Some bug causing bg style edits being made on the current list not updating realtime background info (but still saving??)
Large entity 9-patch from non 32x32 px image scales weird

Geode needs to explain that you can reset or undo