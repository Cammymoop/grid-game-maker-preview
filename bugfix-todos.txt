
Options menu doesn't grab focus for controller compatibility

Stop repeat after bonk prevents further movement at all in discrete mode

Playing create effect needs option to determine if animating entity is active or not during the animation


(low priority)
some tooltips (like slot buttons in conditional editor) have dark text on dark background (Inconsistent or fixed?)

Some bug causing bg style edits being made on the current list not updating realtime background info (but still saving??)
Large entity 9-patch from non 32x32 px image scales weird

Geode needs to explain that you can reset or undo